if (document.querySelector("#passWord")) {
    var ShowPasswordToggle = document.querySelector("#passWord");
    ShowPasswordToggle.onclick = function () {
        document.querySelector("#passWord").classList.add("input-password");
        document.getElementById("toggle-password").classList.remove("d-none");

        const passwordInput = document.querySelector("#passWord");
        const togglePasswordButton = document.getElementById("toggle-password");

        togglePasswordButton.addEventListener("click", togglePassword);
        function togglePassword() {
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                togglePasswordButton.setAttribute("aria-label", "Hide password.");
            } else {
                passwordInput.type = "password";
                togglePasswordButton.setAttribute(
                    "aria-label",
                    "Show password as plain text. " +
                    "Warning: this will display your password on the screen."
                );
            }
        }

    };
}

if (document.querySelector("#confirm_password")) {
    var ShowPasswordToggle2 = document.querySelector("#confirm_password");
    ShowPasswordToggle2.onclick = function () {
        document.querySelector("#confirm_password").classList.add("input-password-2");
        document.getElementById("toggle-password-2").classList.remove("d-none-2");

        const passwordInput2 = document.querySelector("#confirm_password");
        const togglePasswordButton2 = document.getElementById("toggle-password-2");

        togglePasswordButton2.addEventListener("click", togglePassword2);
        function togglePassword2() {
            if (passwordInput2.type === "password") {
                passwordInput2.type = "text";
                togglePasswordButton2.setAttribute("aria-label", "Hide password.");
            } else {
                passwordInput2.type = "password";
                togglePasswordButton2.setAttribute(
                    "aria-label",
                    "Show password as plain text. " +
                    "Warning: this will display your password on the screen."
                );
            }
        }
    };
}